import React from "react";

const ErrorPage = () => {
  <div className="text-red-500 text-3xl min-h-[calc(100vh-400px)] pt-50 text-center">
    <div>Sorry, we are not available rigth now.</div>
    <div>Error: {error}</div>
  </div>;
};

export default ErrorPage;
